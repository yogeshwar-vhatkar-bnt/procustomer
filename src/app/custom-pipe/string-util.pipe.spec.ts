import { StringUtilPipe } from './string-util.pipe';

describe('StringUtilPipe', () => {
  it('create an instance', () => {
    const pipe = new StringUtilPipe();
    expect(pipe).toBeTruthy();
  });
});
